# Reset the taxon and sample IDs

Reset the taxon and sample IDs

## Usage

``` r
reset_ids(ta, keep_prev = FALSE)
```

## Arguments

- ta:

  A tidytacos object.

- keep_prev:

  A logical scalar. Should the previous IDs be kept in a column called
  taxon_id_prev?
