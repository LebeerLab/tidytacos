# Change sample IDs to a given expression

Change sample IDs to a given expression

## Usage

``` r
change_id_samples(ta, sample_id_new)
```

## Arguments

- ta:

  A tidytacos object.

- sample_id_new:

  An expression that evaluates to a unique sample identifier.
